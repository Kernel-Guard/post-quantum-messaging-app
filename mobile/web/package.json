{
  "name": "pqmsg-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "node ./scripts/build-wasm.mjs",
    "stage:wasm": "node ./scripts/stage-wasm-pkg.mjs",
    "dev": "vite",
    "build": "npm run stage:wasm && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.6.0"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "typescript": "^5.7.3",
    "vite": "^6.2.0",
    "vitest": "^4.0.18"
  }
}
